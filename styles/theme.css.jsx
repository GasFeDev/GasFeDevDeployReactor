import { createTheme } from "@mui/material";

const theme3S = createTheme({
    palette: {
        secondary: {
            main: '#ffff',
            light: '#ffff',
            contrastText: '#ffff',
        },
    },
});

export default theme3S